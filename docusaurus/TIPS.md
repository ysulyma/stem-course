- documentation comments
- Cmd+/ to comment out
- Object.assign to set multiple style properties at once

- need to cover `getBoundingClientRect()` somewhere
- Context: how to do `throw new Error()`

- `<pre>{JSON.stringify(obj, null, 2)}</pre>`

- you can use `Ctrl-N` / `Ctrl-P` to navigate VScode menus without the arrow keys
